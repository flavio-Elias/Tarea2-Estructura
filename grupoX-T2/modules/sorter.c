#include "sorter.h"

/// sintaxis generica de qsort() (funcion integrada del quicksort) qsort(arr, n, size, comp) , donde arr: es eñ puntero al primer elemento
// n: es el numero de elementos. size: tamaño de cada elemento. comp: puntero a una funcion que compare.